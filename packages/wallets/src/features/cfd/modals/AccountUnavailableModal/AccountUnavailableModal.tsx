import React from 'react';
import { ModalWrapper } from '../../../../components/Base';
import { AccountUnavailable } from '../../screens';

const AccountUnavailableModal = () => {
    return (
        <ModalWrapper>
            <AccountUnavailable />
        </ModalWrapper>
    );
};

export default AccountUnavailableModal;
