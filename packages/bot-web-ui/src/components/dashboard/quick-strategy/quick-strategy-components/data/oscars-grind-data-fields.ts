import { DURATION_UNIT, DURATION_VALUE, LOSS, PROFIT, STAKE, SYMBOL, TRADE_TYPE, TYPE_STRATEGY } from './data-fields';
import { OSCARS_UNIT } from './data-uniq-input-obj';

const oscars_grind_data_fields = [
    TYPE_STRATEGY,
    SYMBOL,
    TRADE_TYPE,
    DURATION_UNIT,
    DURATION_VALUE,
    STAKE,
    LOSS,
    OSCARS_UNIT,
    PROFIT,
];

export default oscars_grind_data_fields;
