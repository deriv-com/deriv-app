// import { onChangeContractTypeList } from "../contract-type";

describe('onChangeContractTypeList', () => {
    // it('should return', () => {
    //     expect(onChangeContractTypeList()).toHaveProperty('stop_loss');
    // });
    // it('should return', () => {
    //     expect(onChangeContractTypeList()).toHaveProperty('take_profit');
    // });
});

describe('onChangeContractType', () => {
    // it('should return', () => {
    //     expect(onChangeContractType()).toHaveProperty('stop_loss');
    // });
    // it('should return', () => {
    //     expect(onChangeContractType()).toHaveProperty('take_profit');
    // });
});
