import {
    domain_app_ids,
    getCurrentProductionDomain,
    isProduction,
    getAppId,
    getSocketURL,
    checkAndSetEndpointFromUrl,
} from './config';

export default {
    domain_app_ids,
    getCurrentProductionDomain,
    isProduction,
    getAppId,
    getSocketURL,
    checkAndSetEndpointFromUrl,
};
