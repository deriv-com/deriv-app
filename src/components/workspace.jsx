import React from 'react';
import            '../../scratch/sass/_blockly-toolbox.scss';

const Workspace = () => (
    <React.Fragment>
        <div id='scratch_area' />
        <div id='scratch_div' />
    </React.Fragment>
);

export default Workspace;
