import { localize } from '@deriv/translations';

export const title = {
    buy: localize('Purchase Error'),
    cancel: localize('Deal Cancellation Error'),
    contract_update: localize('Contract Update Error'),
    sell: localize('Sell Error'),
};
