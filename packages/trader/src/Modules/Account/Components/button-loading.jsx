import React   from 'react';
import Loading from '../../../templates/app/components/loading.jsx';

const ButtonLoading = (props) => <Loading {...props} is_fullscreen={false} className='initial-loader--btn ' />;

export default ButtonLoading;
