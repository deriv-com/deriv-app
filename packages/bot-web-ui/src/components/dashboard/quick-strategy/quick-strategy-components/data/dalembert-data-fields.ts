import { DURATION_UNIT, DURATION_VALUE, LOSS, PROFIT, STAKE, SYMBOL, TRADE_TYPE, TYPE_STRATEGY } from './data-fields';
import { ALEMBERT_UNIT } from './data-uniq-input-obj';

const dalembert_data_fields = [
    TYPE_STRATEGY,
    SYMBOL,
    TRADE_TYPE,
    DURATION_UNIT,
    DURATION_VALUE,
    STAKE,
    LOSS,
    ALEMBERT_UNIT,
    PROFIT,
];

export default dalembert_data_fields;
