# `component`
> Reusable UI components for Deriv.

## Usage
```jsx
import Button from 'deriv-ui/lib/Button';

const SomeComponent = () => (
    <Button is_disabled primary>
      Hello World
    </Button>
  );
```

## Components
