export type TDocument = {
    additional?: {
        display_name?: string;
        example_format?: string;
    };
    example_format?: string;
    id: string;
    text: string;
    value?: string;
};
