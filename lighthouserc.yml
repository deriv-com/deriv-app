ci:
  collect:
    startServerCommand: npm run serve:safe
    url:
      - 'http://localhost:8080/'
      - 'http://localhost:8080/bot'
    settings:
      emulatedFormFactor: desktop
      maxWaitForLoad: 50000
