import React from 'react';
import { Link } from 'react-router-dom';

const AppStore = () => {
    return (
        <div>
            App Store <Link to='/cashier'>TEST</Link>
        </div>
    );
};

export default AppStore;
