import {
    getAllRoutesConfig,
    getSelectedRoute,
    addRoutesConfig,
    normalizePath,
    findRouteByPath,
    isRouteVisible,
    getPath,
    getContractPath,
} from './route';

export default {
    getAllRoutesConfig,
    getSelectedRoute,
    addRoutesConfig,
    normalizePath,
    findRouteByPath,
    isRouteVisible,
    getPath,
    getContractPath,
};
