export const identity_status_codes = {
    PENDING: 'pending',
    REJECTED: 'rejected',
    VERIFIED: 'verified',
} as const;

export const document_status_codes = {
    PENDING: 'pending',
    REJECTED: 'rejected',
    VERIFIED: 'verified',
} as const;
