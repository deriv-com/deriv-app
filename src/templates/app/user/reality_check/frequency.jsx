import React from 'react';
import Wrapper from './wrapper.jsx';

const Frequency = () => (
    <React.Fragment>
        <Wrapper>
            <p>{it.L('Options trading can become a real addiction, as can any other activity pushed to its limits. To avoid the danger of such an addiction, we provide a reality-check that gives you a summary of your trades and accounts on a regular basis.')}</p>
        </Wrapper>
    </React.Fragment>
);

export default Frequency;
