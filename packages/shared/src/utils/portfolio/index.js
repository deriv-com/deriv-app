import {
    getCurrentTick,
    getDurationUnitValue,
    isEndTime,
    getDurationUnitText,
    getDurationPeriod,
    getDurationTime
} from './portfolio';

export default {
    getCurrentTick,
    getDurationUnitValue,
    isEndTime,
    getDurationUnitText,
    getDurationPeriod,
    getDurationTime
}
